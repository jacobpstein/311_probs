# Large raw + intermediate data (regenerable via pipeline/)
data/raw/
data/prepared.parquet

# Agent / editor tooling — not part of the project
.claude/
.DS_Store
__pycache__/
*.pyc


# Historical raw + intermediate data (regenerable via pipeline/fetch_311_history.py)
data/raw_hist/
data/prepared_hist/

# Local deck exports / one-off documents — not part of the project
deck_files.zip
*.pdf

# Compiled Stan programs (built locally by cmdstanpy)
pipeline/stan/*
!pipeline/stan/*.stan
